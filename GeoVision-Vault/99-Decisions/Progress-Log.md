---
title: Progress Log
type: decisions
status: living
updated: 2026-09-05
---

# Progress Log

One line per working session. Newest at the top. This is how a fresh session (human or AI)
learns where things actually stand — [[Build-Order]] holds the status table, this holds the
narrative.

Format: `YYYY-MM-DD · Module · what shipped · what's next / blocked on`

---

| Date | Module | What shipped | Next / blocked |
|---|---|---|---|
| 2026-09-05 | 05 | **Phone/webcam pairing, discovered as a real bug and closed the same day.** Testing the ESP32 provisioning QR with an actual phone: the QR encodes raw JSON for a human to retype into a captive portal, but a phone's own camera app "helpfully" extracts the JSON's embedded server URL and opens *that* as a link — landing on a bare API endpoint with nothing to render, seen live as a stuck progress bar. Fix, purely additive (the ESP32 QR is untouched): `issue_pairing_token` now also returns a second QR/URL (`pair_page_url`, `build_pair_page_qr` in `app/infrastructure/qr.py`) encoding an actual link, `{server}/pair?code={code}`. `GET /pair` (`app/api/v1/routers/mobile_pair.py`, unauthenticated and unprefixed like `/health`) serves a self-contained HTML+JS page (`app/static/mobile_pair.html`) that claims the code and signs uploads **in the browser**, hitting the exact same `/pair/claim` and `/ingest/images` endpoints real firmware uses. Real constraint found along the way: `window.crypto.subtle` only exists in secure contexts (HTTPS or `localhost`), and a phone on the same Wi-Fi opening `http://<lan-ip>:8000` is neither — so the page implements SHA-256 and HMAC-SHA256 in plain JavaScript instead. Verified three ways before shipping: byte-for-byte identical multipart body construction against Python's own `build_multipart` (confirmed via direct diff), the hash/HMAC output matching Python's `hashlib`/`hmac` and RFC 4231's official test vectors (checked via a throwaway Node script, since this sandbox has no browser), and a self-test the page itself runs on every load that refuses to proceed if the hand-rolled hash is ever wrong. `PairingModal.tsx` shows the new QR alongside the existing ESP32 one, clearly labeled. New tests: `test_qr.py`, `test_mobile_pair.py` (8 total); also fixed an architecture-guard test (`test_transactional_route.py`) that correctly flagged the new router for missing `TransactionalRoute` before it was exempted for the same reason `health.py` already is — no database access at all. `docker/nginx.conf` gained a `/pair` location so the deployed stack (not just dev) can serve it. 448 backend unit tests, 48 frontend tests, ruff/mypy/lint-imports/tsc all clean. | Consider adding `device_type` tracking (esp32/phone/webcam/mini_pc) so the dashboard doesn't call every non-ESP32 device "ESP_..." — raised in conversation, not yet built. |
| 2026-08-29 | 16 | **Module 16 (Deployment) built and verified end to end, same day as the Module 09 serving fix above — deliberately paired, since a deployment that only ever runs the stub would have proven nothing new.** `docker/{backend,worker,dashboard}.Dockerfile`, `docker/docker-compose.yml` (nginx + backend + worker + beat + dashboard, fronting postgres/redis/minio, healthchecks + `depends_on` conditions throughout), `docker/nginx.conf`, self-signed TLS generation, `documentation/{DEPLOYMENT,RUNBOOK,DEMO}.md`, `scripts/{backup,restore}.py`, and `deploy-*`-prefixed tasks in both `Makefile` and `dev.ps1` (ADR-040 explains the prefix — a separate compose file for a separate question, not more `up`/`migrate` names). **Verified for real, all the way through**: built all three images (backend 665 MB with no torch — confirmed by importing torch inside the running container and watching it fail; worker 2.76 GB, CPU-only `torch==2.13.0+cpu`; dashboard 78.5 MB), brought up the full stack, ran migrations and seeding inside the deployed backend image, and confirmed `GET /api/v1/model/status` through nginx over HTTPS reports the real ResNet18 checkpoint live (`"live_classifier":{"architecture":"resnet18","is_stub":false}`, `"preprocessing_matches":true`) — not the stub, and not just a claim. **Five real bugs found running this for real, each one only visible by actually building and booting the stack, not by reading the Dockerfiles**: (1) `.dockerignore`'s bare `models/`/`.venv/`/`node_modules/` lines only match at the context root, not at any depth (unlike `.gitignore`) — `backend/.venv`, `dashboard/node_modules`, and `backend/models/best.pt` were all still entering the build context, ballooning it to 2.37 GB and 15 minutes just to transfer; fixed with explicit `**/`-prefixed patterns for anything that can appear nested, keeping unprefixed patterns only for things that would wrongly also match `ai/src/ai/models/` if wildcarded. (2) `backend/uv.lock` was stale after adding `gunicorn` to `pyproject.toml` earlier the same day — `uv sync --locked` (correctly used in the Dockerfile for reproducible builds) caught it immediately where a plain `uv sync` locally had silently auto-updated the lock instead. (3) `S3ObjectStorage.__init__` builds a `boto3.client()` synchronously — cheap once cached, but the *first* call anywhere in the process pays botocore's service-model-load-from-disk cost inline, and since nothing warmed it at startup (a stale comment in `main.py` literally said "Module 05+ will ensure the object-storage bucket exists" and never did), the first request to touch storage — `/health/ready`, in this case — blocked the event loop long enough to trip gunicorn's 30s worker timeout and get `SIGKILL`ed. Fixed by warming `get_storage(settings)` at app startup via `asyncio.to_thread`, mirroring how `InferenceService.warm_up()` already avoids the identical trap on the worker side. (4) nginx's `upstream { server backend:8000; }` block resolves DNS once at nginx startup and never again; restarting `backend` (exactly what `make deploy-up` does after a code change) left nginx holding a dead IP and returning 502 "Connection refused" until nginx itself was restarted — fixed with Docker's embedded resolver (`resolver 127.0.0.11 valid=10s`) plus a `set $upstream ...; proxy_pass $upstream;` variable, which is what actually forces re-resolution per nginx's own docs (a static `upstream {}` block cannot do this at all in stock nginx). (5) `backend/scripts/seed_db.py`'s `FINE_CLASSES` table still held the pre-[[ADR-Index#ADR-036\|ADR-036]] 10-class scheme (`site_clearing` … `completed`, index 0-9) — `predictions.fine_class_index`'s check constraint was tightened to `0..3` by [[ADR-Index#ADR-039\|ADR-039]] on 2026-08-28, so every seed attempt since the 2026-08-18 rescope would have failed identically; nobody had re-run seeding since, so it went unnoticed until `deploy-seed` ran it for the first time. Fixed to the real 4-class table from `ai/src/ai/configs/classes.yaml`. A sixth, smaller thing worth naming: `deploy-migrate`/`deploy-seed` were first written with a `uv run` prefix, which fails in the runtime image — `uv` is a build-time tool, never copied into the final stage, and the venv's own scripts are already on `PATH`. **440 backend unit tests, 286 ai tests, ruff/mypy/lint-imports all clean, both Makefile and `dev.ps1` updated in lockstep (Rule 2b).** | The two ports (dashboard's own internal nginx and the outer reverse-proxy nginx) are documented in DEPLOYMENT.md but worth remembering if either config is touched again; publish the checkpoint properly (Q10) instead of a gitignored local copy; a real domain + Cloudflare Tunnel (Q4) is the next deployment-adjacent step once the ESP32-CAM is on site. |
| 2026-08-29 | 09 | **The live worker now actually serves the real ResNet18 checkpoint instead of the stub.** Two gaps closed: `ai/inference/service.py`'s `build_service(use_stubs=False, ...)` unconditionally raised `NotImplementedError` — stale since Module 07 finished — and `backend/app/infrastructure/ai/adapter.py`'s `get_inference_service()` never once passed `classifier_weights`/`detector_weights`/`inference_device` from `Settings` into it, so even fixing the first gap alone would have changed nothing. `build_service` now dispatches to `ai.models.registry.load_classifier` when `use_stubs=False`, and requires `classifier_weights` explicitly (`ValueError` if missing, rather than a silent stub fallback that would let a deployment believe it was serving a trained model). **Deliberate decision on the detector side**: with no `detector_weights` configured (Module 08 is still blocked on annotation), the service now runs with `detector=None`, not `StubDetector()`. A stub detector returns a fixed, made-up set of boxes that correspond to nothing in the frame — mixed with a *real* classification, ADR-038's fused formula (`sub_stage_fraction = (confidence + detector_fraction) / 2`) would have treated that fake data as genuine corroboration, silently inflating or randomizing every progress number. `None` degrades the same formula honestly instead: no detections found means `detector_fraction = 0` wherever a class has a checklist configured, which reads as "no evidence yet," not "this is real evidence that happens to be fake." Also found and fixed the same class of bug this project keeps finding: `Settings.model_dir` defaults to an absolute `REPO_ROOT / "models"`, but `.env`'s `GV_MODEL_DIR=./models` is relative — and since the API/worker always run with `backend/` as cwd (Rule 2b), that resolves to `backend/models/`, not the repo-root `models/` the vault's own prose had been describing checkpoints as living in. Confirmed by checking where `GV_LOCAL_STORAGE_PATH=./storage` actually lands (`backend/storage/`, not `outputs/storage/`) — the same relative-to-`backend/` convention, just never previously exercised for `model_dir` because nothing read it until today. Checkpoint copied to `backend/models/classifier/resnet18/v1/best.pt`; `.gitignore` gained a `backend/models/**` rule (the existing repo-root `models/**` rule doesn't match nested paths, and a blanket `**/models/**` would have wrongly also hidden the tracked `ai/src/ai/models/` Python package). Verified end to end: real site photos in, real ResNet18 predictions out (e.g. `Finishing`, confidence 0.67-0.85) with no stub anywhere in the path. New tests: `ai/tests/test_inference.py::TestBuildServiceRealWeights` (5 tests) and `backend/tests/unit/test_inference_adapter.py` (5 tests, the settings-wiring gap specifically). 286 ai tests, 440 backend unit tests. | YOLO annotation remains the only blocker on Module 08 / a real detector; publish the checkpoint properly (Q10) instead of a gitignored local copy. |
| 2026-08-29 | 07 | **The classifier evaluation pipeline went from "produces a checkpoint" to "produces a real, trustworthy comparison table."** `ai/evaluation/run_all.py`'s `_resolve_classifier`/`_resolve_detector` previously fell back to the stub unconditionally, regardless of what `--classifier`/`--detector` was given — a leftover from before real loaders existed. Now wired to a new `ai/models/registry.py`, which reads a checkpoint's own `architecture` field (added to what `trainer.py` saves) and picks `ResNet18Classifier` or `MobileNetV3Classifier` automatically; a checkpoint with no such field (i.e. anything trained before today) defaults to ResNet18, since that was the only backbone that existed. Built the MobileNetV3 side of Module 07 to make that registry meaningful: `ai/models/mobilenetv3.py` (mirrors `resnet18.py`) and `ai/models/common.py` (the freeze/unfreeze helpers, generalized to take a `head_prefix` instead of hardcoding ResNet's `fc.` — MobileNetV3's head lives at `classifier.3.`). Trained it for real under identical conditions to ResNet18: 40 epochs, early-stopped, val macro-F1 0.3662. Evaluated **both** models on the same held-out test split (`outputs/evaluation/backbone-comparison-v1/comparison.csv`): ResNet18 scores higher on validation (0.4603) but *lower* on the held-out test set (macro-F1 0.3805 vs MobileNetV3's 0.4100, top-1 31.7% vs 35.8%) — a genuine validation/test divergence worth its own paragraph in the results chapter, not an accident to paper over. Three real bugs found and fixed running this for real rather than as a smoke test: (1) PyTorch 2.6 tightened `torch.load`'s default `weights_only` to `True`, which broke loading this project's own checkpoints (they deliberately carry class names and other plain-Python metadata alongside the tensor weights) — fixed by passing `weights_only=False` explicitly, safe because these files are never from an untrusted source; (2) `_load_labeled_directory` globbed only `.jpg`/`.jpeg`, so evaluating against `dataset/processed/test/` — which turned out to be 100% `.png` — silently produced "no labelled images" instead of real numbers; (3) the backbone comparison table's `top1_accuracy`/`macro_f1` columns were hardcoded to `None` even when a real classification report had just been computed in the same run, a leftover from when nothing but the stub was ever benchmarked. 282 ai tests (up from 278), ruff/mypy clean. | Publish both checkpoints (Q10); YOLO annotation remains the one blocker left with no code-side workaround. |
| 2026-08-28 | 07 + 08 + Q2/Q7/Q11 | **Module 07 (classifier training) built and actually run against real data — first non-stub result exists.** Shipped: `scripts/split_dataset.py` (discovers "groups" automatically — any folder whose immediate children are the four class names — rather than trusting top-level site-folder names; `dataset/raw/Aldea Grove` turned out to hold three separate houses under one name, which a naive per-site split would have leaked across train/test), `ai/data/transforms.py` + `datamodule.py` (`ConstructionStageDataset` runs every image through the shared Module 06 pipeline once at load, excluding quality-gate rejects, then caches for per-epoch augmentation), `ai/models/resnet18.py` (real torchvision ResNet18, freeze/unfreeze, checkpoint validated against `classes.yaml` on load), `ai/training/{callbacks,trainer,train_classifier}.py` (early stopping, `ReduceLROnPlateau`, AMP with CPU fallback, `WeightedRandomSampler` + inverse-frequency loss weights for the class imbalance, per-run `outputs/runs/<id>/`). Ran for real: 24 epochs, early-stopped, **best validation macro-F1 = 0.4603** — far under the 85% target, and honestly so: Foundation has only 16 training images after the split. Also shipped Module 08's code (`ai/models/yolov8.py`, `ai/training/train_detector.py`, `dataset/labels/detection/data.yaml`) — correct and ready, but cannot train yet since no bounding-box annotation exists. Two ready-to-run Kaggle notebooks (`ai/notebooks/`) close Q7 — deliberately `pip install --no-deps` the `ai` package so Kaggle's GPU torch is never silently replaced by the project's CPU-pinned wheel (ADR-012). Closed **Q11** (ADR-039): predictions are superseded, not deleted — a real migration (partial unique index, since a plain `UNIQUE(image_id)` cannot express "unique among current rows only"), and a stale `fine_class_index BETWEEN 0 AND 9` check constraint fixed to `0 AND 3` in the same migration (60 pre-rescope seed rows deleted, verified all pre-dated ADR-036/038). Closed **Q2** with `backend/scripts/capture_and_upload.py` — reuses `simulate_device.py`'s signing code directly, supports webcam/file/folder-watch sources, reads GPS from photo EXIF. Discovered along the way: 488 of 661 raw images were `.jpg`, but a full 173 were invisible to the pipeline entirely until this session — 123 turned out to be `.png` (`Online Dataset/`) and 50 `.heic` (`Paramjeet/`, iPhone default, `cv2.imdecode` cannot read it at all). **700 backend tests, 276 ai tests, ruff/mypy/lint-imports all clean.** | Foundation-class images remain the highest-leverage next step (P1-9) — the trainer and split script are already correct and will simply produce a better number once more exist. Bounding-box annotation (Module 08) is now the only non-hardware, non-external blocker left with no code-side workaround. |
| 2026-08-27 | dataset + Q17 | **Dataset made accurate and actually usable; test coverage re-verified live.** Ran the OpenCV quality gate (`scripts/audit_dataset_quality.py`, new) over every raw photo: 619/661 scored images pass (93.6%), all 42 rejections blur, none too dark. Sorted the two genuinely uncategorized pockets of `dataset/raw/` by visually classifying each photo (calibrated against already-sorted neighbours from the same capture sessions): 11 loose photos in `Aldea Grove/1` (5 -> Foundation, an empty pre-construction lot; 6 -> Finishing, the same lot's completed house) and 17 loose photos in `Paramjeet` (2 -> Foundation, 15 -> Structural, a mosque's perimeter CHB wall at progressively later stages). Left `Aldea Grove`'s 7 root-level loose photos and its 4-photo `Cleanup Site` folder untouched, as instructed. **Real bug found in the process: 50 of `Paramjeet`'s photos are `.HEIC`** (iPhone default) — `cv2.imdecode` cannot decode HEIC at all, so every one of them was invisible to `load_image`, the quality gate, and would have been invisible to training, despite genuinely being on disk and counted by a naive `find`. Fixed by converting all 50 to `.jpg` alongside their untouched originals (`pillow-heif`, installed ad hoc via `uv run --with pillow-heif`, not a committed dependency). Separately found `Online Dataset/` is entirely `.png`, which an earlier audit pass had missed by only globbing `.jpg`/`.jpeg`. **Final verified per-class count: Foundation 37, Structural 381, Roofing 110, Finishing 122** — Foundation remains the one real shortfall against the team's own 80-130/class estimate (Q5). Also closed **Q17**: re-ran the full backend suite against live Postgres/Redis/MinIO after today's ADR-038 changes — 700 tests passed, 81.03% combined coverage, identical to the 2026-08-18 measurement, confirming the existing 78% `fail_under` threshold needs no change. | Kaggle training notebooks (Q7), phone/webcam capture uploader (Q2), the `predictions.superseded_at` migration (Q11), notifications + feed/locations features (Q13/Q14) — see `Remaining.md`. |
| 2026-08-27 | fix + decisions | **Every item in [[Open-Questions]] §1 answered by the team; Q18 (the fused progress formula) resolved and implemented, closing the single biggest blocker on Modules 07/08/09.** Recorded as [[ADR-Index#ADR-038\|ADR-038]]: `sub_stage_fraction = (classifier confidence + YOLO checklist coverage) / 2`, mapped onto the class's floor-ceiling band; the checklists reuse [[Construction-Stages]]'s own visual-disambiguation table almost verbatim. The contract deadline (which the team wanted as "the main dictator") was deliberately **not** folded into the raw percentage — it already has a home in [[Project-Status-Rules]]'s existing schedule/delay mechanism, and mixing the two would make the AI percentage stop meaning "what the camera can see was built." Implemented: `classes.yaml` (4 classes: `FDN/STR/ROF/FIN`, plus `detection_checklists`), `ai/progress/mapping.py` (`FineClass` narrowed, new `detection_checklist_for`/`macro_stage_bands`), `ai/progress/estimator.py` (`fused_raw_pct`), `backend/app/worker/inference.py` (`_stage_for` now takes confidence + detections), `dataset/metadata/progress_reference.csv` regenerated. A real latent bug surfaced and was fixed in the same pass: `aggregator.py` derived all five macro-stage bands from the classifier's own class table, which only worked before because the retired `Completed` class happened to carry the `approval` tag — with no class predicting `approval` anymore, that band silently vanished until fixed to read `classes.yaml`'s `macro_stages` section directly. `ai/tests/test_aggregator.py` (the thesis's core-contribution test suite) was rewritten with fixtures chosen so the published §8 worked-example table (28.0, 29.3, 30.7, 31.7, 33.5, 33.5) needed **zero renumbering** — only which (confidence, detections) combination produces those numbers changed. **276 ai tests, 435 backend unit tests, ruff/mypy/lint-imports all clean.** Also recorded: Q1 (stage percentages accepted as imperfect; the deadline concern routes to status, not the AI number), Q2 (no ESP32-CAM in hand right now — near-term capture via phone/webcam against the same ingest API), Q4 (Cloudflare Tunnel), Q5 (team estimates 80-130 images/class; actual tally shows Foundation ~30 is the real shortfall, others are close), Q6 (Asia/Manila confirmed), Q7 (Kaggle, needs training notebooks), Q8 (no format requirement), Q10 (GitHub Releases + Kaggle Datasets, both free), Q11 (keep every prediction, mark old ones superseded — migration not yet written), Q13 (build the feed lateral-join + locations index), Q14 (build notifications as a first-class alerting feature). Real dataset audit run this session: `dataset/raw/` totals ~662 images — Foundation ~30, Structural ~366, Roofing ~110, Finishing ~116, ~39 still unsorted — materially different from the vault's prior assumption that all folders were already fully sorted. | Re-run coverage against live services (Q17) with the ADR-038 changes; build the phone/webcam capture tool (Q2) and Kaggle training notebooks (Q7); write the `predictions.superseded_at` migration (Q11); build notifications (Q14) and the feed/locations lateral join (Q13); then Modules 07/08 once Foundation-class volume and YOLO annotation catch up. Full day-by-day breakdown in `Remaining.md`. |
| 2026-08-18 | fix + 15 | **First live E2E run (Q16, closed) found two real bugs no other test had ever caught.** Cleaned up 16 leftover E2E/probe test projects and their throwaway accounts blocking `scripts/seed_db.py`'s wipe step (explicit allowlist by UUID, not a blanket wipe — left the 3 projects and 3 accounts that looked like genuine manual testing by the project owner untouched). Fixed a stale placeholder in `ai/models/stub.py`'s detection class list to match ADR-036's new 10-item list. Then ran `.\dev.ps1 e2e-ui` against the live stack for the first time ever: 2 of 12 tests failed on the very first run — not flaky, not a test bug. `DEV_PASSWORD_HASH` in `seed_db.py` was a placeholder (a literal all-zeros Argon2 digest) since the day it was written; it never verified against any password, so **every seeded login had been broken from Module 03 onward** — invisible to 700 backend tests and the whole session's manual curl checks, because nothing before this had actually logged in as a *seeded* user through the real verification path. Fixed by computing a real hash. That got one step further and hit a second, previously-unreachable bug: the seeded emails used `@geovision.test`, and `email-validator` (behind Pydantic's `EmailStr`) rejects `.test` as an RFC 2606 reserved domain — but only when validating the **response** model, a check login could never have reached before bug one was fixed. Changed to `@gvmail.com`, the domain every other test file already used. Both pinned: `tests/unit/test_seed_db.py` (new, 2 tests) for the hash, the E2E suite itself for the email issue. **12/12 E2E passing**, including the manuscript screenshots (`/`, `/login`, `/register`, `/search`, `/projects/NG_00`) — useful for the thesis as-is. 700 backend tests (up 2), 81.03% coverage, threshold still clears. | [[Open-Questions|Q18]] (fused progress formula) is now the only thing blocking Module 07/08/09 code. |
| 2026-08-18 | audit + domain | **Full-repo audit, then a real classifier/detector rescope.** Audit: backend (ruff/mypy/lint-imports/guard clean, 698 tests, **81.00% coverage** — `fail_under` raised 60→78 to match, closing Q17), ai (ruff/mypy clean, 275 tests, 89.61% coverage, no change needed), dashboard (tsc clean, 1 non-blocking lint warning, 48/48 tests — one flaky timeout under parallel jsdom contention, reproduced clean on rerun — build succeeds). The reported "Registration failed / Request failed with 500" had no code defect: the API process (port 8000) simply was not running, matching ADR-033's own note that Vite reports a dead upstream as a 500 — fixed by starting the API and worker. Also inventoried `dataset/raw/`: 663 images across 4 sites, already bucketed by the team into Foundation/Structural/Roofing/Finishing folders rather than the vault's 10 fine classes. **That mismatch led to a real domain decision, recorded as [[ADR-Index#ADR-036\|ADR-036]] and [[ADR-Index#ADR-037\|ADR-037]]:** the classifier narrows from 10 fine classes to exactly the 4 macro-aligned classes the team already sorted their data into (`foundation, structural, roofing, finishing` — no more `completed`); YOLO's object list is redefined to 10 elements (`wall/CHB, beam, column, rebar, roofing, window, door, tile, railing, lighting`), replacing the old site-activity list (`column, wall, roof, steel_bar, scaffolding, worker, equipment`); and detection stops being purely advisory — it becomes a direct input to a fused progress signal instead of a corroboration-only side channel. The approval handoff also changes: no more automatic `awaiting_inspection` trigger at the 80% ceiling (there's no `completed` class to fire it) — the owner now opens the approval action themselves, whenever they judge it ready. Updated to match: [[Construction-Stages]], [[Progress-Calculation]] (§1 and §5 flagged superseded, §2-§7/§9/§10 unaffected), [[Dataset-Spec]], [[Annotation-Guide]], [[Module-08-YOLO-Detection]], [[Naming-Conventions]]. **Not yet decided:** the actual fusion formula (how classifier class + detections + physical change combine into one number) — tracked as new [[Open-Questions\|Q18]], with a concrete starting proposal recorded there for the team to react to. **No code touched for this decision** — `ai/src/ai/configs/classes.yaml`, `ai/src/ai/progress/mapping.py`, `ai/src/ai/progress/aggregator.py`, and the detection stub all still reflect the retired 10-class/7-object design, correctly, pending Q18. | Get Q18 answered, then implement: `classes.yaml` + `FineClass` enum (4 values), regenerate `progress_reference.csv`, rewrite `ai/progress/aggregator.py` step 1, align `ai/models/stub.py`'s detection stub list (already diverged even from the *old* 7-class list — found during the audit, worth fixing regardless), then re-run the evaluation suite since the §8 worked-example regression test will need new numbers. |
| 2026-08-18 | 15 ◑ | **Module 15 started: everything buildable without hardware or a labelled dataset.** Scoped deliberately around the project's actual current state — ESP32-CAM ordered (P1-1 in flight), a site with written permission arranged and expected ready in 3-4 weeks, some raw classifier images promised but not yet in the repo — rather than blocking on either. Shipped `ai/evaluation/`: `metrics.py` (accuracy, per-class P/R/F1, macro-F1, confusion matrix, mean absolute ordinal error, top-k accuracy, calibration/ECE), `benchmark.py` (latency/throughput measured through the `StageClassifier`/`ObjectDetector` protocols, so it already benchmarks the stub and will benchmark real weights with zero code changes), `detector_eval.py` (mAP@0.5 / mAP@0.5:0.95 by standard all-point interpolation, plus the classifier-vs-object-count agreement experiment from [[Evaluation-Plan]] §3), `progress_eval.py` (MAE / stage-transition lag / monotonicity violations / jitter reduction against a ground-truth curve — pure functions over `ai.progress.aggregator`, no model involved), and `report.py` + `run_all.py` (the `gv-evaluate` CLI, already wired as a console script in `ai/pyproject.toml` from Module 06). Run today with no dataset and no trained weights, `gv-evaluate` reconstructs [[Progress-Calculation]] §8's worked example from scratch and reproduces its exact hand-verified numbers (28.0, 29.3, 30.7, 31.7, 33.5, 33.5 — including the ratchet holding on day 6), reporting 81.4% jitter reduction on that series, and lists every section it cannot yet produce **by name and by reason** (classifier metrics need P1-3/P1-4; the backbone table needs Module 07/08 weights) rather than silently omitting them. **97% coverage on `ai.evaluation`**, 275 ai tests total (up from 197). Also shipped: `fail_under` coverage thresholds actually enforced in CI for both packages (ai's `test-ai` job did not even collect coverage before this); `test_client_contracts.py`, generalising ADR-034's client/server enum-drift technique from just `ProfessionalRole` to the stage filter, status filter, camera face, and member-role lists — with the deliberately-asymmetric ones (`archived` excluded from the status filter, `owner` unassignable via the role dropdown) tested as intentional, not as gaps to fill; `scripts/export_openapi.py` and `scripts/export_erd.py`, both introspecting the live app/ORM rather than hand-copying the vault's diagrams, so they cannot drift from what is actually deployed (53 documented paths, 19 tables, confirmed by running them); a Playwright E2E scaffold (`tests/e2e/`) for the visitor and owner journeys plus a manuscript-screenshot spec, written against the exact seeded data in `scripts/seed_db.py` and confirmed to typecheck, but **not executed against a live stack this session** — Docker Desktop needed virtualization enabled, which happened only near the end of the session; two k6 load scripts (`tests/load/ingest.js`, byte-for-byte replicating `simulate_device.py`'s hand-built multipart signing since the signature covers the raw body bytes k6's own multipart helper does not expose; `tests/load/api-read.js` for the unauthenticated read path); and [[Hardware-Test-Log]], the template the 72h soak/battery-curve/offline-buffering tests will fill in once hardware and site are actually in hand. 692 backend tests (up from ~635), 275 ai tests. One deliberate deviation from the vault's original plan, worth recording as its own decision: `scripts/screenshot_dashboard.py` was specified as Python; it shipped as a Playwright *test* in `tests/e2e/` instead, because writing it that way avoids installing and maintaining a second browser-automation stack for one script. | Run `.\dev.ps1 e2e-ui` once the stack is up and seeded, to actually execute the Playwright journeys for the first time. Then [[Module-16-Deployment]]. 07/08 remain dataset-bound; 13 remains hardware-bound - both now report their own gap through `gv-evaluate` rather than silently blocking Module 15. |
| 2026-08-15 | fix | **Registration rejected the "Project manager" role.** The dashboard's dropdown was written from memory, not from `ProfessionalRole`: `project_manager` is not a value the server defines (it has `manager` and `project_handler`), and `foreman` and `surveyor` were missing altogether. Choosing the invented option returned 422 and created no account. Replaced with all ten enum values in enum order, with readable labels, and pinned by `tests/unit/test_role_options.py`, which compares the TSX literal against the enum in both directions - restoring the old list reddens all three assertions ([[ADR-Index#ADR-034]]). Also quieted the realtime subscriber: with Redis absent it logged a **full traceback every five seconds**, burying the log a developer is actually reading; now one traceback, then a single line per attempt backing off 5s->30s. **679 backend, 48 frontend.** | [[Module-15-Testing-and-Evaluation]]. Worth a sweep for other server contracts restated in the client. |
| 2026-08-15 | fix | **The API did not start under its own default configuration.** Reported from the dashboard as "Registration failed - Request failed with 500"; the endpoint was fine, the server was not running. `_start_realtime` referenced `RealtimeSubscriber` at runtime while the import sat under `if TYPE_CHECKING:`, so startup raised `NameError` and uvicorn exited. Nothing caught it: the branch runs only when `task_queue_backend == "celery"`, the default, and every test uses `"logging"` and returns one line earlier. Fixed by importing the class at runtime, and pinned by `tests/unit/test_app_boots.py` - both backends, the lifespan actually entered and exited, and a startup that survives an absent Redis. Reverting the fix reddens three of the eight. **676 backend tests.** Recorded as [[ADR-Index#ADR-033]], whose rule outlives the bug: a config guard whose test value differs from its default has a branch no test reaches. | [[Module-15-Testing-and-Evaluation]]. Also worth a look: `storage_backend` and `nonce_cache_backend` have the same test/production asymmetry. |
| 2026-08-15 | 12 ✅ | **Module 12 finished.** Added the rest of the owner surface: the devices page (per-camera weight, unpair with a confirmation that says the images are kept), collaborators (invite by username or email, change role, remove), invitations, profile editing with the public/private toggle, the report modal (period and format, replacing the fixed weekly PDF), blueprint upload, signed-in header navigation, and the **capture lightbox** — photograph, predicted stage, confidence, top-five probabilities, and the detection boxes overlaid on it. **48 frontend tests**, 668 backend, 197 ai; strict TS, ESLint clean, and the public landing page still **78.7 kB gzipped** because every owner screen is its own chunk. Two details worth keeping. The **overlay uses percentages**, because detection boxes are stored normalised — the same numbers land correctly on a 224 px thumbnail and a 1600 px original, whereas pixels would be right on exactly one rendering and silently wrong everywhere else; a test pins the resolution independence. And **`CaptureStrip` takes an optional `onSelect`**: the public page passes none and stays a plain strip, the owner folder passes one and each capture opens the lightbox — passing the capability rather than branching on a `mode` flag leaves the public surface *incapable* of opening a view it should not have. **One page was deliberately not built (Q14):** `/notifications` has no endpoints. The contract lists them, the table, entity and repository all exist, and Modules 09 and 10 are already writing rows — so notifications are being recorded and never shown. Building a page against a missing endpoint would have been worse than leaving it out and saying so. | [[Module-15-Testing-and-Evaluation]], then [[Module-16-Deployment]]. Everything not blocked by hardware or the dataset is now done. |
| 2026-08-15 | 12 ◑ | **The owner dashboard's spine: sign in, create a project, run it.** Scoped deliberately — this is the largest module in the project and the session had a budget, so what shipped is the path that makes the dashboard real and the defense demo work, whole rather than half-finished, with the remainder listed in the module note as genuinely absent rather than half-wired. Delivered: real auth (`lib/auth.ts`, one transparent refresh on a 401, then sign-out rather than a retry loop), session context with a `RequireAuth` guard that waits for the token check instead of bouncing a signed-in user to `/login` on every hard refresh, `/me`, the create-project form with a live **permanent** code preview, the folder workspace, the pairing modal, and **`useRealtime`** — the hook Module 14 deferred, now that the caches it patches exist. **45 frontend tests** (up from 36), TypeScript strict, ESLint clean, build green; the owner surface is its own chunk so the public landing page stays at 78 kB gzipped. Three things worth defending. **Every action button reads the server's `permissions` block**, never a role inferred in the client — hiding a button is not security since the API enforces it too, but offering a viewer an action that returns 403 is a lie the UI tells, and the tests pin that a *missing* permission is denied so it fails closed. **The pairing modal confirms itself** on `device.paired` and still works with the socket down: the countdown runs and the camera appears via the folder's 60-second poll — realtime makes it feel finished, polling is what makes it correct. **Approval is deliberate**: a dialog stating it finalises at 100%, requiring typed inspection notes, saying it is recorded against a name (ADR-007), with the ring reading "AI estimate" until then. The realtime hook patches progress, status and approval in place and *invalidates* for events whose rows the client cannot rebuild from the payload — a capture needs its signed thumbnail URL, a device its derived liveness. | Finish Module 12 (devices/members pages, invitations, notifications, profile editing, asset upload, report modal, lightbox), then [[Module-15-Testing-and-Evaluation]] and [[Module-16-Deployment]]. |
| 2026-08-15 | fix | **Two defects closed, both the same shape: work that had already succeeded being silently undone.** **Q12 / ADR-031:** `get_session` committed in a `yield` dependency's exit code, which FastAPI runs *after the response is delivered* — measured at 6.9 ms, so a client reading its own write got a 404. A `TransactionalRoute` now commits after the handler and before Starlette sends; `get_session` only rolls back; every database-touching router carries the route class. Verified live: **5/5 writes durable before the response, and the immediate read-back returns 200** where it used to 404. The regression test drives the ASGI app directly and asserts the order of the commit against `http.response.start`, because that ordering *is* the property and no HTTP client can observe it — `ASGITransport` awaits teardown, which is exactly why the suite was blind for eleven modules. A second test fails the build if a router is added without the route class. **ADR-032:** Module 05 promised device auth failures were "audited server-side", and the generic 401 is uninformative precisely because the detail was meant to live in that trail — but `_deny` wrote the row and the ensuing rollback took it away. Proved first: three refused uploads produced **zero** rows. `_deny` now commits before raising, safe because device auth runs before the handler and the audit row is the only pending write. The general rule, worth remembering: **any audit row describing a refusal must be committed by whatever refuses**, since the refusal rolls the request back. **668 backend tests** (up 8), 197 ai, 36 frontend; mypy clean, 4 contracts kept. | [[Module-12-Owner-Dashboard]] — nothing blocks it now. |
| 2026-08-15 | 14 ✅ | **Realtime: the dashboard can stop refreshing.** Event port, per-process connection hub, Redis pub/sub fan-out, `/ws` with JWT auth and per-project authorization, and a reconnecting typed client with backoff, jitter, and a heartbeat. **42 new tests** — 660 backend, 197 ai, 36 frontend — mypy clean, 4 contracts kept. Four decisions. **A producer announces; it never delivers**: a capture is scored by a Celery worker that holds no socket and never will, so the port keeps every producer free of Redis and publishing is deliberately total — the prediction is already committed, and turning a Redis hiccup into a failed upload would trade a real outcome for a cosmetic one. **One pattern subscription (`project:*`)** rather than tracking per-project subscriptions as browsers come and go, which would be a second distributed-state problem to keep correct; the hub already ignores events for projects it holds no sockets for. **A refused subscription is silent and audited** — saying "denied" confirms the project exists, the same disclosure the REST layer answers 404 to avoid. And **the connect-time project list is a fast path, not the authority**: a long-lived socket outlives an invitation being accepted, so anything absent from it is re-checked against the database. Most of the hub's tests are about *not leaking* — a hub that leaks sockets does not fail a test, it fails a deployment three days later with a memory graph nobody can explain, so cleanup is asserted on every exit path including the abrupt closes that are normal when a phone leaves coverage. Built **before** Module 12 rather than after: the transport needs nothing from the owner dashboard, and 12's pairing modal closes on `device.paired`. Three things are deferred with reasons in the module note — the `useRealtime` cache-patching hook (it patches devices, reports, and the pairing modal, none of which exist yet), the two-worker fan-out and 50-socket load runs (Module 15 owns multi-process testing; the code path is built for it, Redis from the start, precisely because the in-process shortcut is invisible under `--workers 1`), and full socket round-trip tests (Starlette's `TestClient` drives the app on its own event loop and cannot share the rolled-back test transaction, so the endpoint's security decisions are tested directly and the protocol is tested on the client). | [[Module-12-Owner-Dashboard]] — its remaining dependencies (11, 10, 14) are all done. **Fix Q12 first.** Then 13 (firmware, needs hardware), 15, 16. |
| 2026-08-15 | 11 ✅ | **The public face of the system exists.** Every anonymous route from spec section A: homepage feed with URL-backed filters, the public project folder (progress ring, five stage bars, timeline, captures, remarks, handler, map links), public profiles, search, contact, auth placeholders, and a 404. **19 component tests**, TypeScript strict with `exactOptionalPropertyTypes`, ESLint clean, production build green. The tests worth reading are the ones about *what a visitor is told*: the figure is labelled "AI estimate" and only reads "Verified" at 100 %; the Approval bar says it requires an inspection; a private profile renders exactly two lines, asserted on `textContent` so a future field cannot slip in; and a private project reads as "not available" rather than as an error, because a visitor must not be able to distinguish a private project from one that never existed. **Building the UI surfaced a systemic backend gap and fixed it:** image payloads carried `thumb_key` — a storage key no browser can resolve — and nothing anywhere carried a URL, so no photograph could be displayed on any surface, including the Module 12 lightbox still to come. Payloads now carry a signed `thumb_url`, produced at the presenter (presigning is local computation, not a round trip) with a key that fails to sign simply absent, so a storage hiccup costs a picture rather than the page. Verified live: 12 captures, all signed. **Two gaps logged rather than papered over (Q13):** the feed omits `owner` and `latest_image` that the spec's card calls for — adding them needs a lateral join, and an N+1 on the anonymous homepage is the worst place for one — and `/public/search` returns no locations index, so that tab is derived from project matches client-side. **Bundle:** the landing page is 77 kB gzipped after moving the chart into its own module and lazy-loading the routes; before the split, Recharts rode along on the homepage at 186 kB. `MiniMap` was deliberately not built and MapLibre uninstalled rather than left as an unused dependency — 200 kB for one pin on a page that already links to Google Maps and OSM and shows copyable coordinates. | [[Module-14-Realtime]], then [[Module-12-Owner-Dashboard]]. **Q12 must be fixed before 12.** Playwright's visitor journey belongs to [[Module-15-Testing-and-Evaluation]]. |
| 2026-08-15 | 10 ✅ | **Module 10 finished: the scheduled jobs that keep derived state honest.** Four beat tasks — `projects.refresh_status` (6 h), `remarks.emit_system` (6 h), `devices.sweep_offline` (30 min), `reports.cleanup_expired` (daily) — plus the automatic-remark message table from [[Project-Status-Rules]] as a **pure domain service**. **832 tests** (635 backend, 197 ai), mypy clean, 4 contracts kept. The rules get 15 unit tests against a table of numbers with no database in sight, which is what makes the thresholds arguable in a defense; the jobs get 14 integration tests for the parts only PostgreSQL can show. **Deduplication is deliberately not in the rules.** They report what is true *now*; the job checks whether the same thing was said in the last 72 h and stays quiet if so. Mixing the two would make a rule untestable without a database, and the window is a delivery concern rather than a fact about the project. Four behaviours worth defending: **status is written only when it moves** (the column exists to keep project lists cheap — rewriting every row every six hours churns the table and its indexes for nobody, and a test asserts `updated_at` is untouched); **archived and approved projects are never nagged**, because both were settled deliberately and complaining is noise about a decision already made; **one live camera keeps a site reporting** — the device offline threshold is 6 h but the whole-site alert is 48 h and fires only when *every* paired camera is silent, since waking an owner over one missed capture teaches them to ignore the alerts that matter; and **report cleanup deletes the file before the row**, leaving the row alone if storage is unreachable so the next run retries — deleting it would orphan the blob permanently, because nothing else records that key. Two fixture findings worth remembering: `ProjectRepository.add` does not persist `last_capture_at` or `archived_at`, and `DeviceRepository.add` does not persist `last_seen_at` — all three are set by the ingest, archive, and heartbeat paths through `update`, so a fixture that needs them has to go the same way the application does. | [[Module-11-Public-Dashboard]] — unblocked, and now the largest remaining chunk. **Q12 still blocks Module 12.** 07/08 remain dataset-bound. |
| 2026-08-14 | 10 ◑ | **Reports: the system can now produce a document.** Module 10 was deliberately **split at its own seam** — its title names report generation *and* a beat schedule for status/remarks, which share nothing but a module number. The reports half shipped whole rather than half-shipping both. Delivered: period rules as a pure domain service, the assembled `ReportData` bundle, three matplotlib figures, a **9-section ReportLab PDF**, a two-table CSV, `reports.generate` on its own queue, and four endpoints (request · list · status · download). **803 tests** (606 backend, 197 ai), mypy clean, 4 contracts kept. A real 5-page, 114 KB PDF renders inside a unit test, and the required disclaimer is asserted with ReportLab's page compression **disabled** — a naive byte search on a compressed stream finds nothing and would have passed vacuously, which is the sort of test that certifies nothing. Three decisions. **Periods are always complete and always in the project's timezone**: a weekly report run on a Wednesday covers the previous Monday–Sunday, because a partial period makes the progress curve appear to flatten for reasons that have nothing to do with the site; and a Manila day starts at 16:00 UTC the day before, so a naive UTC range reports the wrong days entirely. **The CSV is one file with two tables** rather than a ZIP, because the format enum is `pdf|csv` and a ZIP would be a third format in all but name. **An empty period still renders a valid report** saying so — "no captures in three weeks" is one of the most useful things a report can say, and precisely when somebody wants a document to show. The layers contract caught the one real architectural error: `ReportData` started in the application layer, and the renderers are infrastructure (ReportLab, matplotlib), so infrastructure was importing application — the direction that ends with renderers calling repositories. It now lives in the domain beside `ReportPeriod`, which is what keeps the builders pure functions from a domain aggregate to bytes (ADR-030). | The **maintenance half of Module 10**: `derive_status` on a schedule, automatic system remarks with dedup, the device offline sweep, and Celery **beat**. Or [[Module-11-Public-Dashboard]], which is unblocked. **Q12 still blocks Module 12.** |
| 2026-08-14 | 09 ✅ | **Module 09 finished: the system now answers for itself over HTTP.** Shipped the remaining surface — image detail with prediction, detections and signed URLs; `/prediction`; `/reprocess`; `/history`; `/progress`; `/timeline`; `/recompute`; `/model/status`; `/models`; and `POST /predict`. **765 tests** (568 backend, 197 ai), mypy clean, 4 import contracts kept. Then ran it **end to end against live PostgreSQL + Redis + MinIO + a real Celery worker: 39/39 checks.** A simulated camera paired, uploaded four geotagged captures, all four were scored, project progress moved to **27.0 %** (`framing`), recompute reproduced it exactly with no duplicate rows, `/model/status` reported a reachable worker on `cpu` at 25 ms mean, `POST /predict` answered in a 73 ms round trip and stored nothing, and reprocess cleared and re-scored one image without duplicating it. Four decisions. **ADR-026:** `/predict` and `/model/status` need a model, but the API may never import torch (ADR-011) — so they became a bounded round trip to the worker over a **third `interactive` queue**, because sharing `inference` would put the live defense demo behind whatever backlog of captures exists. **ADR-027:** image routes moved under their project, matching what Module 05 already did for devices — a project-less path has to look the image up before it can decide whether the caller may know it exists, and a 403 where a 404 belonged leaks other people's capture history. **ADR-028:** the contract said recompute is "manager+" but the permission matrix had no row for it and no existing permission fit, so `progress:recompute` was added properly rather than overloading `project:edit`. **ADR-029 is the one worth reading:** the E2E found a bug **no test could see**. Every Celery task calls `asyncio.run`, which closes its event loop; the database engine was a pooled process-wide singleton, so the second task checked out an asyncpg connection whose loop was dead. It failed from inside the driver as `'NoneType' object has no attribute 'send'`, images silently stopped being scored after the first two, and the reported latency inflated from 27 ms to 3 707 ms as retries piled up. The worker now uses an unpooled engine and disposes it per task. Also fixed: `dev.ps1` and the `Makefile` both still pointed at `app.infrastructure.tasks.celery_app`, deleted when the tasks moved to `app.worker` — `dev.ps1 worker` could not start at all. And the config tests turned out to read the developer's `.env`, so "are production settings valid?" silently meant "on this machine today"; they are hermetic now. **One defect logged, not fixed (Q12):** `get_session` commits in a `yield`-dependency's exit code, which FastAPI runs *after* the response is delivered — measured at **6.9 ms**, so a real network client that reads its own write gets a 404. The suite is blind to it because `ASGITransport` awaits teardown before returning. It affects every write endpoint since Module 03 and the fix touches every router, so it belongs to the foundation, not here — but it must be fixed before Module 12, whose dashboard creates a project and immediately navigates to it. | [[Module-10-Reports-and-Remarks]] or [[Module-11-Public-Dashboard]] — both unblocked. **07/08 remain dataset-bound** but are now a weights swap behind the `StageClassifier` protocol, not an integration. Fix **Q12** before Module 12. |
| 2026-08-14 | 06 ✅ | **Preprocessing pipeline, quality gate, and thesis Figure 6.** Composable YAML-driven steps, CLAHE + white balance, bilateral denoise, letterbox resize, 4-point calibration, and a before/after strip generated from a synthetic scene so the figure exists months before the dataset does. **113 AI tests**, no network/DB/GPU/dataset required. ~88 ms/image on CPU. Three things beyond the spec. First, a **config fingerprint** (ADR-025): the spec's skew defence was "both sides read the same YAML", which prevents accidental divergence but *detects* nothing — edit the config after training and the model is served through a pipeline it never saw, silently. Module 07 now stamps the fingerprint into the checkpoint and Module 09 refuses a mismatch. Second, `ai/progress/constants.py` finally exists, and the vault's claim that it is "imported by both `ai/` and `backend/`" turned out to be impossible — the backend excludes `geovision-ai` to keep torch out of the API process — so a CI script now **parses both files with `ast`** and fails the build on drift (ADR-023). Third, resize and denoise swapped order on measurement: 30.1 ms vs 4.0 ms for the bilateral filter, ~23 % off the whole pipeline (ADR-024). A test also caught a real design flaw: the textbook occlusion heuristic (large flat region = obstruction) **rejects legitimately smooth façades** — fresh concrete, rendered finishes — silently discarding good captures. It now requires low texture *and* intensity unlike the rest of the ROI, compared against the ROI excluding that region so a dominant occluder cannot become its own reference. Separately fixed a Module 05 defect surfaced when Docker stopped: the backend suite required Redis despite ADR-021 promising otherwise; test settings now pin the in-memory cache and 509 backend tests pass with Docker fully down. | [[Module-07-Classifier-Training]] — **blocked on the dataset (P1-3, P1-4)**. Nothing further is buildable without labelled images; the critical path is now entirely calendar-bound. |
| 2026-08-14 | 05 ✅ | **Device pairing and image ingestion — the system can now receive photographs.** Shipped pairing tokens + QR, HMAC-signed ingest, server-side naming, idempotency, race-free daily sequencing, device health, and `scripts/simulate_device.py` with failure injection. **509 tests green.** The simulator ran end to end against live PostgreSQL + Redis + MinIO: four geotagged captures stored under their correct keys, duplicates recognised, all four injected faults rejected with an identical 401. Two findings worth recording. First, **the vault contradicted itself**: [[Device-Pairing-Protocol]] said store the device secret *hashed* and also verify with `HMAC_SHA256(device_secret, …)` — impossible, because a MAC key must be recoverable. Implemented as written, every camera would have failed authentication and it would have looked like a firmware bug. Now encrypted at rest (ADR-020) and the protocol note is corrected. Second, the advisory lock guarding sequence allocation keyed on Python's `hash()`, which is **randomised per process** — two uvicorn workers would have taken different locks and serialised nothing, so two images would share a filename and one would silently overwrite the other in storage. Invisible to every single-process test; fixed with a stable digest and pinned to a literal (ADR-022). Also: `GV_JWT_SECRET_KEY` must be set explicitly when running uvicorn locally — the ephemeral fallback is generated per process, and uvicorn's Windows launcher imports the app more than once, so tokens get signed and verified with different keys. | [[Module-06-AI-Preprocessing]] — pure OpenCV/Albumentations, no server or dataset dependency. |
| 2026-08-14 | env | **Docker Desktop installed; Module 05 unblocked.** Cleared a `docker-desktop` WSL distro wedged in `Uninstalling` (which would have broken a fresh install), then installed Docker 29.6.2 with the WSL data root on `F:\Docker\wsl` - `--installation-dir` turns out to be silently ignored by 4.86, so ~3.4 GB of program files stayed on C: while the part that actually grows went to F:. Put the compose PostgreSQL behind a `db` profile: with both it and the native service bound to 5433, Docker mapped the port while the native server answered, so no error appeared and you simply could not tell which database a query reached. **Exercised the S3 storage backend against real MinIO for the first time** - it had been written blind against a service that did not exist (ADR-018) and passes. Also fixed the readiness probe, which built a throwaway boto3 client per call; client construction alone blew the 3 s budget against a healthy MinIO, so it now goes through the configured backend the app actually uses. | [[Module-05-Device-Pairing-and-Ingestion]] |
| 2026-08-13 | 04 ✅ | **Projects, folders, collaboration, and the public surface.** Shipped the Create Project form, the full folder payload (progress, five stage bars, timeline, devices, members, remarks, assets, plus a caller-specific `permissions` block), invite/accept/role-change/remove, reference uploads with magic-byte validation, remarks CRUD, and the anonymous surface (feed, public project page, timeline, search, contact). **413 tests green.** Two decisions worth defending: storage now sits behind a port with local and S3 backends (ADR-018), which unblocked this module without Docker *and* hands Modules 05/10 both backends for free; and the public project view is a **separate response model** rather than a filtered one (ADR-019), so a field added later cannot leak. Added a `contact_messages` table because a contact form that discards messages is broken rather than deferred. Caught a layering violation while writing it — the first `SubmitContactMessage` reached into `infrastructure` — and rebuilt it behind a repository. | [[Module-05-Device-Pairing-and-Ingestion]] — **needs Redis**, so Docker is now genuinely blocking. |
| 2026-08-13 | 03 ✅ | **Auth, profiles, and the permission machinery.** Audited the spec first and found 4 security gaps → ADR-015 (token-type separation; a refresh token was usable as an access token), ADR-016 (per-account failed-attempt throttle alongside per-IP), plus `jti` on every token and avatar upload deferred to M04 where storage lives. Also built the pieces Modules 04-16 need: `api/deps.py` as a composition root with all 14 repository providers, `require_permission()` returning a resolved `AccessContext`, an audit logger whose action vocabulary already covers M04/M05, an injectable `Clock`, and a shared pagination schema. **327 tests green.** Three bugs the tests caught: a TYPE_CHECKING-only `User` import made FastAPI silently treat every authenticated caller as anonymous; the per-identifier rate-limit key could never work because slowapi runs its key function before the handler; and `to_public_profile()` redacted the owner's own profile. The import contract also caught `core/exceptions.py` dragging FastAPI into the application layer → ADR-017. | [[Module-04-Projects-and-Folders]]. Docker/MinIO needed for its asset upload. |
| 2026-08-13 | 02 ✅ | **Database schema complete.** Installed PostgreSQL 16 natively on `F:\PostgreSQL\16` (port 5433) rather than waiting on Docker — see [[Local-Environment-Setup]]. Shipped: 20 enums, 4 self-validating value objects, 21 entities, 14 repository Protocols + 14 SQLAlchemy implementations, 18 tables (63 indexes, 26 checks, 29 FKs), a reversible migration, and an idempotent seed (3 users, 4 projects, 2 devices, 60 images, 30 days of snapshots). **198 tests green** (154 unit + 44 integration against real PostgreSQL). Alembic autogenerate had omitted `CREATE EXTENSION` and enum `DROP TYPE`, both of which would have broken CI and any rollback; patched and verified by running upgrade→downgrade→upgrade. Test-caught bug: `Image.build_filename` used `astimezone(tz=None)` (server-local, not UTC), which would have filed every capture into the wrong aggregation window on a Manila-time host. | [[Module-03-Auth-and-Users]]. Docker still needed before Module 05 (Redis/MinIO). |
| 2026-08-13 | 01 ✅ | **Foundation & environment setup complete.** Audited the module spec first, which surfaced 4 blocking design gaps → ADR-011 (packaging topology: `ai` as a src-layout library + 3 backend dependency groups), ADR-012 (uv + committed lockfiles, ruff-only, CPU-pinned torch), ADR-013 (Celery on Windows), ADR-014 (`stringzilla<4`, albumentations 2.x). Shipped: full repo tree, git + remote, `.gitattributes` before first commit, backend Clean-Architecture skeleton with settings/logging/exception envelope, `/health` + `/health/ready`, async Alembic scaffold, 4 enforced import contracts, `ai` package skeleton (torch 2.13+cpu), Vite/React/TS-strict dashboard, dev compose stack with MinIO + Postgres extension bootstrap, CI (6 jobs), `Makefile` + `dev.ps1`, no-TensorFlow guard. **59 tests green** (46 backend / 11 ai / 2 dashboard). Two real bugs caught by the new tests: the app factory ignored injected settings, and `.env` list parsing crashed at startup. | Install **Docker Desktop** (Q9), then [[Module-02-Database-Schema]]. In parallel: answer Q1–Q5, order hardware, start dataset collection. |
| 2026-08-12 | — | Obsidian vault created: full finalized architecture, domain model, progress algorithm, API contract, pairing protocol, 16 module build-specs, ADR-001…010, evaluation and thesis mapping. Root `ARCHITECTURE.md` + `README.md` point here. | Start [[Module-01-Foundation-Setup]]. In parallel: answer Q1–Q5 in [[Open-Questions]], order hardware, begin dataset collection. |

---

## Session hygiene

At the **start** of a session: read [[00-START-HERE]] → the top row of this log → the module
note you are working on.

At the **end** of a session: add a row here, update the status table in [[Build-Order]], and
record any new decision in [[ADR-Index]] or any new unknown in [[Open-Questions]].

## Related
[[00-START-HERE]] · [[Build-Order]] · [[ADR-Index]] · [[Open-Questions]]
